# ⚠️ STALE COPY — DO NOT EDIT THIS FOLDER

The live, current app is in the newest revision folder under `VERSIONS\`:

    VERSIONS\VPM_Tracker_Rev_2026.07.08-r1\   (or a later -r# / newer-dated rev)

**Always edit the newest `VERSIONS\VPM_Tracker_Rev_<date>-r<n>\` folder.**
The Python files at THIS top level (`ui\`, `models\`, `utils\`, `tracker_app2.py`)
are an older snapshot and are no longer the source of truth. Editing them has no
effect on the app you actually run (`Open VPM Tracker.cmd` inside the rev folder).

The approved planning documents (`*_PLAN.md`) DO live here at the top level — those
stay put. Only the app *code* moved into the rev folders.

Marked 2026-07-08.
